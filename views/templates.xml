<odoo>
    <data>
        <record id="menu_form_cantidad" model="website.menu">
            <field name="name">Cantidad</field>
            <field name="url">/cantidad</field>
            <field name="parent_id" ref="website.main_menu"/>
            <field name="sequence">55</field>
        </record>

        <template id="create_cantidad" name="Report incidence">
            <t t-call="website.layout">
                <div id="wrap" class="oe_structure oe_empty">
                    <section>
                        <div>
                            <button class="sumar">Sumar</button>
                            <input class="cantidad" type="text" readonly="readonly"/>
                            <button class="restar">Restar</button>
                        </div>
                    </section>
                </div>
            </t>
        </template>
    </data>
</odoo>